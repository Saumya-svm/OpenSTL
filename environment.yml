name: OpenSTL
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - numpy
  - hickle
  - pip
  - python
  - pytorch
  - xarray==0.19.0
  - pip:
    - fvcore
    - mkl < 2022
    - nni
    - scikit-image
    - timm
    - tqdm
prefix: /opt/anaconda3/envs/simvp